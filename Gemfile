source 'https://rubygems.org'

# Bundle edge Rails instead: gem "rails", github: "rails/rails", branch: "main"
gem 'rails', '~> 7.2.2', '>= 7.2.2.1'

gem 'bootstrap', '~> 5.3.1'
gem 'chartkick'

gem 'dartsass-sprockets'
gem 'devise-i18n'

gem 'devise'
gem 'enumerize'
gem 'jquery-rails'
# The original asset pipeline for Rails [https://github.com/rails/sprockets-rails]
gem 'sprockets-rails'

# Use the Puma web server [https://github.com/puma/puma]
gem 'puma', '>= 5.0'
# Use JavaScript with ESM import maps [https://github.com/rails/importmap-rails]
gem 'importmap-rails'
# Hotwire's SPA-like page accelerator [https://turbo.hotwired.dev]
gem 'turbo-rails'
# Hotwire's modest JavaScript framework [https://stimulus.hotwired.dev]
gem 'stimulus-rails'
# Build JSON APIs with ease [https://github.com/rails/jbuilder]
gem 'jbuilder'
# Use Redis adapter to run Action Cable in production
# gem "redis", ">= 4.0.1"

# Use Kredis to get higher-level data types in Redis [https://github.com/rails/kredis]
# gem "kredis"

# Use Active Model has_secure_password [https://guides.rubyonrails.org/active_model_basics.html#securepassword]
# gem "bcrypt", "~> 3.1.7"

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem 'tzinfo-data', platforms: %i[windows jruby]

# Reduces boot times through caching; required in config/boot.rb
gem 'bootsnap', require: false

# Use Active Storage variants [https://guides.rubyonrails.org/active_storage_overview.html#transforming-images]
gem 'image_processing', '~> 1.2'

group :development, :test do
  # See https://guides.rubyonrails.org/debugging_rails_applications.html#debugging-with-the-debug-gem
  gem 'debug', platforms: %i[mri windows], require: 'debug/prelude'

  # Static analysis for security vulnerabilities [https://brakemanscanner.org/]
  gem 'brakeman', require: false

  # Omakase Ruby styling [https://github.com/rails/rubocop-rails-omakase/]
  gem 'rubocop-rails-omakase', require: false

  # Check for security vulnerabilities in gem dependencies
  gem 'bundler-audit'

  # Interactive debugger for Ruby
  gem 'byebug', platforms: %i[mri mingw x64_mingw]

  # Modify environment variables in tests
  gem 'climate_control'

  # Load environment variables from .env files
  gem 'dotenv-rails'

  # Test data generator with factories
  gem 'factory_bot_rails'

  # Generate fake data for tests and seeds
  gem 'faker'

  # Simplify form filling in feature specs
  gem 'formulaic'

  # Auto-run specs when files change
  gem 'guard-rspec'

  # Helper for launching external applications
  gem 'launchy'

  # Run tests in parallel
  gem 'parallel_tests'

  gem 'pg'
  gem 'pghero'
  gem 'pg_query'
  # Combine pry with byebug
  gem 'pry-byebug'

  # RSpec mocks for ActiveModel
  gem 'rspec-activemodel-mocks'

  # Additional collection matchers for RSpec
  gem 'rspec-collection_matchers'

  # Its syntax for RSpec
  gem 'rspec-its'

  # RSpec for Rails applications
  gem 'rspec-rails'

  # Auto-retry flaky specs
  gem 'rspec-retry'

  # Ruby code style checker and formatter
  gem 'rubocop'

  # Rubocop extension for Faker
  gem 'rubocop-faker'

  # Rubocop extension for performance optimization
  gem 'rubocop-performance'

  # Rubocop extension for Rails
  gem 'rubocop-rails'

  # Rubocop extension for RSpec
  gem 'rubocop-rspec'

  # Rubocop extension with Shopify's Ruby style guide
  gem 'rubocop-shopify'

  # Shoulda matchers for testing
  gem 'shoulda-matchers'

  # Static code analyzer with quality reports
  gem 'rubycritic'

  # IDE tools for code completion and inline documentation
  gem 'solargraph'

  # Rails support for Solargraph
  gem 'solargraph-rails'
end

group :development do
  # Use console on exceptions pages [https://github.com/rails/web-console]
  gem 'web-console'
end

group :test do
  # Use system testing [https://guides.rubyonrails.org/testing.html#system-testing]
  gem 'capybara'
  gem 'selenium-webdriver'

  # For testing controller templates
  gem 'rails-controller-testing'
end
